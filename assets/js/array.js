

var arr = [1,2,3,4,5,6,7,8,9,0];
console.log(arr);

var arrNames = ['Marten', 'Lisa', 'Paula'];
console.log(arrNames);

/**
 * Adds an Entry to the array and creates a new index
 */
arrNames.push(
    // String || Number || Object || Array || null || undefined || bool
);

/**
 * Returns the last entry of the array and removes it
 * @type {string}
 */
var lastEntry = arrNames.pop();